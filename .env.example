DATABASE_URL="postgresql://user:password@localhost:5432/juz40_socialcheck"

# OpenAI API key — used for profile-screenshot and document OCR (active provider, src/lib/openai.ts)
OPENAI_API_KEY="change-me"

# Google Gemini API key — alternative provider, currently unused (src/lib/gemini.ts).
# Swap the import in src/lib/profileOcr.ts and src/lib/ocr.ts to switch back.
GEMINI_API_KEY="change-me"
